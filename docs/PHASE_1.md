# Phase 1 — Integration Proof

## Completed

- Created and verified a full pre-overhaul duplicate of the live classic Wix site.
- Identified the actual editorless Wix-managed Headless project and renamed it `BDCT Headless Staging`.
- Configured the repository for staging site ID `afd7fd4e-9ed7-43dc-b074-1613b3a78208` and companion app ID `70d88af5-972d-4f5a-8d78-6acdd7f8ab53`.
- Confirmed the Wix-hosted staging URL: `https://h6s-92e874502bd0c4-bdcustomtattoos.wix-site-host.com/`.
- Audited the live Joker and Linda Wix Form schemas.
- Confirmed both forms already upsert contacts and retain their existing Wix automations.
- Confirmed neither initial inquiry asks for reference-image uploads.
- Audited the existing Wix Blog and retrieved a complete live post including its Ricos rich-content body.
- Created and corrected the dedicated OAuth connection from the staging frontend to the existing Black Diamond Wix site.
- Added the proposed homepage, Joker and Linda copy to the staging source.
- Added the current artist form field targets and option values to the staging source without altering the live schemas.
- Added staging-only `noindex` and `robots.txt` protection.
- Added the Wix-managed Astro adapter, Wix project configuration, build scripts, and release scripts.
- Added automated GitHub Actions validation and deployment with post-release route checks.
- Verified in GitHub Actions that dependency installation, `astro check`, and the production Astro build all succeed.

## Deployment gate

The deployment workflow is complete and linked to the actual Wix staging project. It requires the repository Actions secret `WIX_CLI_API_KEY`, generated by the Wix account owner with the **Wix CLI - Git Integration** permission. Once present, the existing workflow performs all of the following without further code changes:

1. Authenticates the Wix CLI.
2. Pulls the Wix-managed runtime environment.
3. Builds through the Wix CLI.
4. Releases the frontend to Wix staging hosting.
5. Verifies `/`, `/joker`, `/linda`, `/integration-status`, `/blog`, and a complete `/post/...` route.
6. Verifies that the staging artist pages contain no reference-image upload field.

## Remaining controlled business test

After the read-only deployment proof passes:

1. Submit one labeled test inquiry through Joker's existing form ID.
2. Verify the submission, CRM contact, existing automation and `/thankyou-joker` route.
3. Submit one labeled test inquiry through Linda's existing form ID.
4. Verify the submission, CRM contact, existing automation and `/thankyou-linda` route.
5. Delete the temporary test records after documenting the result.

Those submissions intentionally create temporary records in the live Black Diamond CRM and may send live automation messages, so they remain a separate controlled test.

## Live systems that remain authoritative

- Wix Contacts / CRM
- `Project Qualification-Joker Ink`
- `Project Qualification-Linda G`
- Existing Wix Automations attached to those forms
- Existing Wix Blog and all `/post/...` content
- Existing Wix Media Manager
- Existing Wix business dashboard

## Current copy status

The source includes the proposed copy for owner review. It is present on staging but is not locked as final production copy until approved.
